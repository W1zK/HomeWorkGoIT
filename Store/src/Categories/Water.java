package Categories;

public class Water extends Items {
    public Water(int id, String name, double value, String other, double price) {
        super(id, name, value, other, price);
    }

    @Override
    public void show() {
        super.show();
    }
}
